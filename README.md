"npm install" and "npm update" to update the node modules (even if you already did it, I added another one)

Once all modules are installed, "npm start" to start server, then navigate to localhost:3000

